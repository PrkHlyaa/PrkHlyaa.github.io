name: Web Scraping

on:
  workflow_dispatch:
  schedule:
    - cron: '*/2 * * * *' 

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests beautifulsoup4

    - name: Run Web Scraping Script
      run: python scrape.py

    - name: Check status of data.json
      run: git status

    - name: Commit and Push Changes
      run: |
        git config --global user.name "PrkHlyaa"
        git config --global user.email "zahra19hilyatul@gmail.com"
        git add "E:\\Kuliah\\Proyek 1 Pengembangan Perangkat Lunak Dekstop\\PrkHlyaa.github.io\\data.json"
        git commit -m "Update data.json"
        git push
